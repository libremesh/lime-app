import "@testing-library/jest-dom";

describe("Mesh wide upgrade Page", () => {
    beforeEach(() => {});

    afterEach(() => {});

    it.skip("should show loader when eupgrade status is loading", async () => {});
    it.skip("should show no updates available component when no upgrade is available", async () => {});
    it.skip("should show ready to start transaction when new firmware is downloaded", async () => {});
    it.skip("should show node error when an error is returned", async () => {});
});
